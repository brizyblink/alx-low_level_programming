#include <stdio.h>

/**
 * main - tests function that prints if integer is positive or negative
 * Return: Always return 0
 */

int main(void)
{

	int i;

	i = 0;
	positve _or_negative(i);

	return (0);
}
